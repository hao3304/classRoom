query GetTracing($type: String!, $id: String!, $stime: Int64!, $etime: Int64!) {
  tracing(input: { type: $type, id: $id, stime: $stime, etime: $etime }) {
    lng
    lat
    speed
    course
    accu
    time
  }
}
